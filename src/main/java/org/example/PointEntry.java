package org.example;

import org.example.model.Point;

public class PointEntry {
  Point point;
  long distance;

  public PointEntry(Point point, long distance) {
    this.point = point;
    this.distance = distance;
  }
}
